# configs/accel-packages-only.config
# 仅编译硬件加速相关软件包，不编译完整固件

# =============================================================================
# 1. 基础系统配置 (最小化)
# =============================================================================
CONFIG_TARGET_mediatek=y
CONFIG_TARGET_mediatek_filogic=y
CONFIG_TARGET_ARCH_PACKAGES="aarch64_cortex-a53"
CONFIG_LINUX_6_6=y

# 不编译固件镜像 (节省时间)
# CONFIG_TARGET_ROOTFS_INITRAMFS is not set
# CONFIG_TARGET_ROOTFS_SQUASHFS is not set

# =============================================================================
# 2. 核心加速模块 (必选)
# =============================================================================
# Hardware NAT
CONFIG_PACKAGE_kmod-mediatek-hnat=y
CONFIG_MTK_FAST_NAT_SUPPORT=y
CONFIG_MTK_PPE_OFFLOAD=y

# WARP Acceleration
CONFIG_PACKAGE_kmod-warp=y
CONFIG_MTK_WARP_SUPPORT=y

# Flow Offloading
CONFIG_NF_FLOW_TABLE=y
CONFIG_NF_FLOW_TABLE_HW=y
CONFIG_PACKAGE_kmod-ipt-offload=y

# FullCone NAT
CONFIG_PACKAGE_kmod-ipt-fullconenat=y
CONFIG_PACKAGE_iptables-mod-fullconenat=y
CONFIG_PACKAGE_nftables-mod-fullconenat=y

# =============================================================================
# 3. 用户空间工具 (推荐)
# =============================================================================
# TurboACC LuCI Interface
CONFIG_PACKAGE_luci-app-turboacc-mtk=y
CONFIG_PACKAGE_luci-i18n-turboacc-mtk-zh-cn=y

# MTK Wireless Management
CONFIG_MTK_WIFI_DRIVER=y
CONFIG_PACKAGE_kmod-mt7915e=y
CONFIG_PACKAGE_kmod-mt7981-firmware=y
CONFIG_PACKAGE_mt7981-wo-firmware=y
CONFIG_PACKAGE_mtwifi-cfg=y
CONFIG_PACKAGE_luci-app-mtwifi-cfg=y
CONFIG_PACKAGE_luci-i18n-mtwifi-cfg-zh-cn=y

# =============================================================================
# 4. 依赖包 (自动处理，但可显式声明确保编译)
# =============================================================================
CONFIG_PACKAGE_kmod-crypto-hw-safexcel=y
CONFIG_PACKAGE_kmod-nft-offload=y
CONFIG_PACKAGE_kmod-nft-fullcone=y

# =============================================================================
# 5. 禁用无关组件 (加速编译)
# =============================================================================
# 不编译内核镜像
# CONFIG_KERNEL_BUILD_IMAGE is not set

# 不编译默认软件包 (只编译上面指定的)
# CONFIG_ALL_NONSHARED is not set
# CONFIG_ALL_KMODS is not set

# =============================================================================
# 6. 包编译设置
# =============================================================================
CONFIG_PACKAGE_INSTALL_ALL_DEPENDENCIES=y
CONFIG_IB_STANDALONE=y